apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend1
  labels:
    app: frontend1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend1
  template:
    metadata:
      labels:
        app: frontend1
    spec:
      dnsPolicy: ClusterFirst
      dnsConfig:
        nameservers:
          - 8.8.8.8
          - 8.8.4.4
      containers:
      - name: frontend1
        image: kubregistry2.azurecr.io/frontend1:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
      imagePullSecrets: 
        - name: kubernetessecretname