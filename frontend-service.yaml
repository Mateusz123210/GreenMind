apiVersion: v1
kind: Service
metadata:
  name: frontend1-service
spec:
  selector:
    app: frontend1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  sessionAffinity: ClientIP